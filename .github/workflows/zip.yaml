name: Create Zip Package
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: 📦 Generate .zip
        uses: byteever/action-build-zip@master
        with:
          filename: 'DelBuonoPaolini_Alberto_Esercizi.zip'
